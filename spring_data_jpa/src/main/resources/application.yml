spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/spring_data_jpa
    password: rootroot
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: ture
        format_sql: true
  data:
    web:
      pageable:
        default-page-size: 10
logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type : trace # parameter??? ? ?? ?? ??